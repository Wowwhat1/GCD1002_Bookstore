{% extends 'base.html.twig' %}

{% block title %}Edit Publisher
{% endblock %}

{% block body %}
	<div class="container">

		<h1>Edit Publisher</h1>

		{{ include('publisher/_form.html.twig', {'button_label': 'Update'}) }}

		<a class="btn btn-dark" href="{{ path('app_publisher_index') }}">back to list</a>

		{{ include('publisher/_delete_form.html.twig') }}
	</div>
{% endblock %}
